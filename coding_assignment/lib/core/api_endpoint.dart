// Utility class that holds constant endpoint URLs for making API requests.

class EndPoints {
  // Private constructor to prevent instantiation of this class.
  const EndPoints._();
  // This constant string holds the URL for the API endpoint to fetch stock scan date data.
  static const String getScanDate =
      'http://coding-assignment.bombayrunning.com/data.json';
}
